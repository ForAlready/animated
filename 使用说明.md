# Ria 俏皮待机动画

## 直接观看

1. 双击本目录的 `打开动态角色.cmd`，浏览器会自动打开本机预览。
2. 等待角色载入；首次加载约 83 MB 模型，需要几秒。播放库已保存在本地，无需联网。
3. 点击“面部近景”，观察眨眼、嘴角和眉毛变化。拖动鼠标旋转，滚轮缩放。
4. “暂停”可停止播放；拖动“动作进度”逐帧观察；速度支持 0.25～1.5 倍。
5. 关闭启动时的命令窗口可停止本地预览服务。不要直接双击 index.html，需要通过启动入口访问。

## 动作内容

新制作的 12 秒待机循环，按每秒 30 帧采样，包含轻微呼吸、肩部微动、缓慢歪头和转头、嘴角微笑、脸颊与眉毛的细微变化。1.7、4.4、8.2、10.7 秒附近双眼眨眼，6.2 秒附近左眼 wink。双臂保持放松下垂，脚部不移动。该版本以微动作和微表情为主，没有舞蹈或大幅挥手。

## 导出与继续编辑

`Ria_Suit13_Playful.glb` 是整套角色：头、身体、头发、Suit13 衣服配饰、嵌入贴图、骨骼及 `Playful_Idle_12s` 动画均在一个文件中。预览页“保存带动画模型”下载的是同一个文件，可复制给支持 GLB 骨骼动画和形变动画的软件继续使用。

本次实际验证环境是本地 Edge 的三维预览，已经检查全身、眨眼、wink 画面；其他软件的材质和动画导入效果未逐一验证。FModel 继续用于原资源查看、导出，这个新动画由独立预览入口播放。

## 已知范围

- 这是基于原骨骼与原有表情形变新制作的程序动画，不是提取出的游戏原动画。
- 沿用之前的眼睛与裙色通用材质适配；最终渲染与游戏仍有差异。
- 为避免闭眼时的白片、睫毛交叉，动画版本省略了无法正确还原的 EyeSlash、Eyeshadow、Eyelid 独立辅助层，保留脸部、眼球和脸部贴图中的眼线。源文件未删除或修改。
- 为控制预览开销，仅保留本段动画使用的 8 个表情目标。完整表情库与姿态修正目标仍在原始导出模型中。
- 衣物跟随骨骼；尚未添加头发、裙摆的物理模拟和复杂接触修正。
- 启动脚本使用本机现有 Python 路径，换电脑时需要调整启动环境；GLB 本身不依赖该路径。

播放器使用本地 Three.js 0.170.0，许可证在 vendor/LICENSE。动画播放接口参考：https://threejs.org/docs/#api/en/animation/AnimationMixer 。
