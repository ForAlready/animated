# Ria 俏皮待机动画

## 直接观看

1. 双击本目录的 `打开动态角色.cmd`，浏览器会自动打开本机预览。
2. 等待角色载入；首次加载约 83 MB 模型，需要几秒。播放库已保存在本地，无需联网。
3. 点击“面部近景”，观察眨眼、嘴角和眉毛变化。拖动鼠标旋转，滚轮缩放。
4. “暂停”可停止播放；拖动“动作进度”逐帧观察；速度支持 0.25～1.5 倍。
5. 关闭启动时的命令窗口可停止本地预览服务。不要直接双击 index.html，需要通过启动入口访问。

## 动作内容

新制作的 12 秒待机循环，按每秒 60 帧采样，包含轻微呼吸、肩部微动、缓慢歪头和转头、嘴角微笑、脸颊与眉毛的细微变化。1.7、4.4、8.2、10.7 秒附近双眼眨眼，6.2 秒附近左眼 wink。双臂保持放松下垂，脚部不移动。该版本以微动作和微表情为主，没有舞蹈或大幅挥手。

## 导出与继续编辑

`Ria_Suit13_Playful.glb` 是整套角色：头、身体、头发、Suit13 衣服配饰、嵌入贴图、骨骼及 `Playful_Idle_12s` 动画均在一个文件中。预览页“保存带动画模型”下载的是同一个文件，可复制给支持 GLB 骨骼动画和形变动画的软件继续使用。

本次实际验证环境是本地 Edge 的三维预览，已经检查全身、眨眼、wink 画面；其他软件的材质和动画导入效果未逐一验证。FModel 继续用于原资源查看、导出，这个新动画由独立预览入口播放。

## 已知范围

- 这是基于原骨骼与原有表情形变新制作的程序动画，不是提取出的游戏原动画。
- 沿用之前的眼睛与裙色通用材质适配；最终渲染与游戏仍有差异。
- 为避免闭眼时的白片、睫毛交叉，动画版本省略了无法正确还原的 EyeSlash、Eyeshadow、Eyelid 独立辅助层，保留脸部、眼球和脸部贴图中的眼线。源文件未删除或修改。
- 为控制预览开销，仅保留本段动画使用的 8 个表情目标。完整表情库与姿态修正目标仍在原始导出模型中。
- 衣物跟随骨骼；尚未添加头发、裙摆的物理模拟和复杂接触修正。
- 启动脚本使用本机现有 Python 路径，换电脑时需要调整启动环境；GLB 本身不依赖该路径。

播放器使用本地 Three.js 0.170.0，许可证在 vendor/LICENSE。动画播放接口参考：https://threejs.org/docs/#api/en/animation/AnimationMixer 。


## 第二版修复：脚部遮挡与眨眼

- 补上独立趾甲网格和鞋内低于 0.10 米的脚部三角形隐藏，保留手指甲。继续沿用 Suit13 的身体遮挡遮罩，不使用整个人体缩小的方式遮掩穿模。
- 眨眼改用平滑曲线：约 75 毫秒闭合、25 毫秒保持、170 毫秒睁开；wink 为 140 / 75 / 280 毫秒。消除旧曲线峰值处突然反向的问题。
- 减弱常驻眯眼、眉部和脸颊叠加强度，wink 达到完整闭合，脸颊与嘴角使用更缓慢的配合动作。
- 已检查脚部近景、正面及背面/两侧的代表性时刻、半闭合/闭合/睁眼阶段；这些检查不代表任意新增动作都不会穿模。未加入实时碰撞求解。
- 修复作用于本目录的动画 GLB 和 Suit13_fixed 部件；FModel 旧预览补丁及其他服装没有在本次同步重建。

若浏览器仍显示旧效果，按 Ctrl+F5 强制刷新，或关闭旧预览再从启动入口打开。修复图为 feet_fixed.png、closed.png，侧背面检查图为 side.png、opposite.png、back.png。


## 实时裙摆碰撞

动态预览现已默认开启“裙摆碰撞”，可点击同名按钮关闭/开启比较。更新后请按 Ctrl+F5 刷新页面。

当前实现范围：裙摆与左右大腿的骨骼跟随碰撞包络，每帧修正碰撞顶点，解除接触后平滑回到原形。暂停、拖动进度、关闭碰撞时均可正确恢复。已验证 5,880 个裙摆顶点的处理、正常播放、加大碰撞范围的接触测试和开关恢复。

这是实时防穿透适配，不是完整布料动力学：尚不包含布料自碰撞、手臂/场景物体碰撞、风力、头发碰撞，也不保证大幅新增动作下不会穿插。腿部碰撞使用近似包络，不是精确皮肤三角网格。上衣仍使用原骨骼和遮挡处理。

碰撞效果只在此预览程序中运行。下载的 GLB 保留原动画，不包含运行时碰撞效果；要转到其他软件，需要重新配置相应碰撞或另行烘焙。请保留本目录 index.html、cloth-collision.js 和 vendor 文件夹共同使用。


## 纹理清晰度更新

已启用硬件支持范围内最高 16× 各向异性过滤、三线性 mipmap、最高 DPR 2（跟随屏幕，不强制低分辨率屏幕超采样），并减弱灯光和曝光。原预览没有雾效，继续保持无雾。

皮肤不再统一设置 0.65 粗糙度，改用已有 Body_R / Head_Game_R 贴图；GLB 已同步嵌入。这里使用通用 PBR 贴图适配，未完整还原游戏皮肤着色器；原材质的 Roughness=3/2 是自定义着色器参数，没有直接当作 PBR 数值使用。

本机五份已导出 T_Role1_Body_D 均为 2048×2048，未找到更高分辨率版本。提高过滤质量无法增加纹理像素。重新导出只有在原资源保留了更高分辨率 mip 时才有帮助；更改 UV 布局还需要重烘焙贴图，不能靠重新导出自动提升细节。

已在浏览器验证 32 张纹理过滤设置、DPR 2 上限、无雾及身体/脸部粗糙度贴图加载，结果见 quality_validation.json。请 Ctrl+F5 刷新旧页面。


## 随视角飘动

默认开启“柔软摆动”。用鼠标拖动角色视角，发梢与裙摆会产生轻微滞后和回弹，停止拖动后回稳。“飘动强度”范围 0～1.5，可调低或关闭。暂停角色动画后仍可拖动视角体验摆动。

此效果驱动 89 根头发骨骼和 42 根裙摆骨骼，固定发根及裙腰，保留骨骼长度与原始轮廓。使用限幅弹簧与阻尼，先计算摆动，再处理裙摆腿部碰撞。已测试视角响应、角度上限及停止输入 6 秒后回稳，并检查鼠标拖动后的画面。

这是为交互预览制作的视角驱动效果，不是把相机运动当成真实风力。当前柔软处理覆盖头发与裙摆，上衣仍由原骨骼驱动；尚无头发与肩部碰撞、布料自碰撞。效果实时运行，不包含在下载的 GLB 中。更新页面请 Ctrl+F5。


## 面部与偏暗修正

提高中性环境补光，加入随相机移动的柔和正面补光，并将曝光调整为 1.08，减少转动视角时的暗面。脸部法线强度降低至 0.4，缓和眼周过强凹凸；不改变皮肤粗糙度贴图。

眉毛属于原始脸部底色贴图，保留原眉形。上眼睑的粗黑线也是原图中的眼线，不是额外的眉毛模型。本次没有重绘眉毛或眼线，而是修正其显示光照与凹凸感。已检查全身、面部近景和眨眼画面，播放器未报告错误。

这些设置属于预览渲染层；下载 GLB 不携带预览灯光或本次法线强度覆盖。更新后 Ctrl+F5 刷新。


## 所有已找到服装的切换

打开预览后，使用左侧“切换服装”下拉框，无需分别启动程序。载入期间会显示状态，完成后保留相机视角；“保存带动画模型”下载当前选择的服装。切换时释放上一套模型的图形资源。旧页面请 Ctrl+F5 刷新。

当前可选择：Suit01、Suit01A、Suit03、Suit04、Suit05、Suit09、Suit10、Suit12、Suit13、Suit14，共 10 组。Suit13 已包含在内。Suit02、Suit06、Suit07、Suit08、Suit11 在当前包内未找到对应服装模型（Suit06 存在遮挡/辅助资源），列表中禁用并标注，不用复制其他衣服凑数量。

特殊情况：
- Suit05 未找到本套上衣，使用 Suit13 上衣作为明确标注的临时搭配，并非还原完整原套装。
- Suit04、Suit05、Suit09、Suit14 未找到本组独立鞋部件；其中部分有袜子。缺鞋的组恢复可见脚部，不冒充原鞋。
- Suit12 未找到专用遮挡图，保留原身体，作为预览版本；复杂视角可能仍有服装贴合或穿插问题。
- 通用动画、眼睛材质、贴图过滤、面部补光与发梢摆动继续生效。只有 Suit13 启用已调校的裙摆碰撞和裙摆摆动，其他套装相应碰撞按钮禁用。

每套模型文件均为 `Ria_套装编号_Playful.glb`，位于本目录。10 个 GLB 通过结构验证，已逐套加载、截图并检查往返切换。模型/材质仍为通用预览适配，不保证所有细节与游戏一致。需要补齐未找到的服装，须有包含那些模型的额外游戏资源包。
