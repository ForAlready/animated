const {run}=require('./physics-test-harness.cjs'),fs=require('fs');
run(async page=>{const result=await page.evaluate(async()=>{const p=physics,a=actions,results=[];for(const enabled of [false,true,false,true]){p.enabled=enabled;p.reset();a.play('idle',true);const times=[];let last=await new Promise(requestAnimationFrame);for(let i=0;i<75;i++){const now=await new Promise(requestAnimationFrame);if(i>=15)times.push(now-last);last=now}times.sort((a,b)=>a-b);results.push({enabled,median:times[30],p95:times[57]})}return {results,scope:'Headless Edge SwiftShader, actual requestAnimationFrame intervals including rendering; not hardware GPU performance'}});fs.writeFileSync(__dirname+'/physics_render_timing.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));});
